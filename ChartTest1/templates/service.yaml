apiVersion: v1
kind: Service
metadata:
  name: {{ include "Helloword.fullname" . }}
  labels:
    {{- include "Helloword.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Helloword.selectorLabels" . | nindent 4 }}
